import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollContentContainer: {
    alignItems: 'center',
    alignSelf: 'stretch',
  },
  scrollView: {
    alignSelf: 'stretch',
  },
});
